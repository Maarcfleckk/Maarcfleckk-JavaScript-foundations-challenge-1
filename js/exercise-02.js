/* 
    1. Declare a variable using let with any name you like, but without a value.
    2. On a separate line, assign the variable the String: "Moo!"
    3. Log out this variable to the terminal using console.log
    
    Repeat the above steps but try using a const variable. Comment this new part of code and explain what is happening.
*/

// Using let:
// Step 1: Declare a variable using let with any name you like, but without a value.
let duckLet; // Declare a variable 'duckLet' without an initial value.

// Step 2: On a separate line, assign the variable the String: "Moo!"
duckLet = "Moo"; // Initialize the 'duckLet' variable with the string "Moo".

// Step 3: Log out this variable to the terminal using console.log
console.log(duckLet); // Use the 'duckLet' variable and log its value to the console.

// Using const:
// Step 1: Declare a variable using const with any name you like, but without a value.
//const duckConst; This will result in an error, as const variables must be initialized when declared.

// To correct the error and follow the DINI method:
// Step 2: On a separate line, assign the variable the String: "Moo!"
const duckConst = "Moo"; // Initialize the 'duckConst' variable with the string "Moo."

// Step 3: Log out this variable to the terminal using console.log
console.log(duckConst); // Use the 'duckConst' variable and log its value to the console.
