/* 
   Create a constant variable of each of the following primitive types:
   - String
   - Number
   - Boolean
   - Null
   - Undefined

   Log out each variable to the terminal at the end.
*/
const primitiveString = "John Doe";
const primitiveNumber = 3.1415;
const primitiveBoolean = false;
const primitiveNull = null;
const primitiveUndefined = undefined;

console.log(primitiveString);
console.log(primitiveNumber);
console.log(primitiveBoolean);
console.log(primitiveNull);
console.log(primitiveUndefined);
